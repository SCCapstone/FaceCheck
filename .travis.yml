os: osx
osx_image: xcode10.3
sudo: true
language: node_js
node_js:
  - "stable"
# cache:
#   directories:
#     - FaceCheckApp/node_modules

before_install:
  - pwd 
  - cd FaceCheckApp

install:
  - npm install

script:
  - npm run lint
  - npm test